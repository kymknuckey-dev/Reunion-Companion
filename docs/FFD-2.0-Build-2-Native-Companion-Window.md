# FFD 2.0 Build 2 — Native Companion Window

Native AppKit WKWebView window over the unchanged FFD 1.9 RC1 genealogy engine. Local navigation stays in-app; external links open in the default browser; closing hides the window; Quit owns backend shutdown.
