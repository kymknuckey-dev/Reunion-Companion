# FFD 2.0 Build 1 — macOS Application Foundation

Objective: double-click `Reunion Companion.app`, use Companion, and quit it without opening Terminal.

The genealogy engine remains FFD 1.9 RC1. Build 1 adds only a native macOS lifecycle shell.
It deliberately continues to use the development repository's `.venv` and existing browser UI.
