# Probe samples

Keep controlled Reunion probe packages outside Git unless distribution is legally and personally appropriate.

Suggested local naming:

```text
Probe-00
Probe-01
...
Probe-16
```

Each probe should differ from the previous probe by exactly one documented change.
